{
  "$schema": "../gen/schemas/mobile-schema.json",
  "identifier": "ios",
  "description": "Capabilities for the DBReader iOS app",
  "platforms": ["iOS"],
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:default",
    "dialog:allow-open",
    "filebridge:default",
    "notification:default"
  ]
}
